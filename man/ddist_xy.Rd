% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ddist_xy.R
\name{ddist_xy}
\alias{ddist_xy}
\title{Calculate dyadic distances between points \code{i} and \code{j} from separate datasets}
\usage{
ddist_xy(
  x = NULL,
  y = NULL,
  ids = NULL,
  coords_x = c("longitude", "latitude"),
  coords_y = c("longitude", "latitude"),
  crs = c(4326, 4326),
  crs_transform = FALSE,
  new_crs = NULL
)
}
\arguments{
\item{x}{an object of class \code{data.frame} or \code{tibble}.}

\item{y}{an object of class \code{data.frame} or \code{tibble}.}

\item{ids}{variables uniquely identifying geospatial points in data \code{x} and \code{y}. Can be of type numeric, integer, character, or factor}

\item{coords_x}{names of the numeric coordinate variables in data \code{x}. Defaults to c("longitude", "latitude")}

\item{coords_y}{names of the numeric coordinate variables in data \code{y}. Defaults to c("longitude", "latitude")}

\item{crs}{valid EPSG's for your coordinates in data \code{x} and \code{y}. Defaults to c(4326, 4326).}

\item{crs_transform}{a logical value indicating whether to transform the CRS. Defaults to FALSE}

\item{new_crs}{a valid EPSG for a new CRS. See \code{rgdal::make_EPSG()} or \url{https://epsg.org/home.html}}
}
\value{
a long \link[tibble]{tibble} with dyads and dyadic distances incl. a distance unit indicator
}
\description{
This function calculates the geodesic distance between any dyads (pairs of points \code{i} and \code{j}) from datasets \code{x} and \code{y} and stores the result in a long tibble
}
\author{
Jeppe Vierø
}
